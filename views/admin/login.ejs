<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      background-color: #fde6f3; /* soft pink sama kaya halaman API */
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      font-family: "Poppins", sans-serif;
    }

    .card {
      background-color: #ffe3f3; /* light pink sama */
      border: none;
      border-radius: 12px;
      box-shadow: 0 0 15px rgba(238, 136, 199, 0.25);
    }

    .card-header {
      background-color: #ee88c7; /* pink utama */
      color: white;
      border-radius: 12px 12px 0 0;
      padding: 18px;
    }

    .form-control {
      background-color: #fff0f7; 
      border: 1px solid #ee88c7;
      border-radius: 8px;
    }

    .form-control:focus {
      background-color: #ffeafd;
      border-color: #d86eae;
      box-shadow: 0 0 6px #f3a6d1;
    }

    .btn-dark {
      background-color: #ee88c7;
      border: none;
      border-radius: 8px;
      font-weight: 600;
    }

    .btn-dark:hover {
      background-color: #d86eae;
    }

    a.text-muted {
      color: #bb5d9c !important;
      font-weight: 500;
    }

    a.text-muted:hover {
      color: #ee88c7 !important;
    }
  </style>

</head>
<body>

  <div class="col-md-4 col-lg-3">
    <div class="card shadow-lg">
      <div class="card-header text-center">
        <h3>Admin Login</h3>
      </div>

      <div class="card-body p-4">

        <% if (error) { %>
          <div class="alert alert-danger" role="alert">
            <%= error %>
          </div>
        <% } %>

        <form action="/admin/login" method="POST">
          
          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input type="email" class="form-control" id="email" name="email" required>
          </div>

          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" class="form-control" id="password" name="password" required>
          </div>

          <div class="d-grid">
            <button type="submit" class="btn btn-dark">
              Login
            </button>
          </div>

          <div class="text-center mt-3">
            <a href="/" class="text-muted small">Kembali ke Halaman User</a>
          </div>

        </form>

      </div>

    </div>
  </div>

</body>
</html>
